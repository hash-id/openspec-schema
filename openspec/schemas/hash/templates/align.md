## Summary

<!-- Final state after the loop ends. -->
- Cycles run: <n>
- Outcome: <clean-walkthrough | user-signed-off>
- Known MEDIUM: <count> • Known LOW: <count>

## Cycle 1

### ALIGN

<!-- One line per finding: `[MECHANICAL|DECISION] <finding> -> <fix applied | user decision | deferred>`. Omit a severity heading if it had no findings. -->

#### HIGH
-

#### MEDIUM
-

#### LOW
-

### WALKTHROUGH

<!-- One line per file, in build order: proposal.md -> each specs/**/*.md -> design.md -> tasks.md. Format: `<file> -> <user's answer, verbatim or summarized>`. "Nothing" is a valid answer but must be recorded per file - do not collapse multiple files into one line. -->
-

<!-- Add a `## Cycle N` block (ALIGN + WALKTHROUGH) for each cycle actually run. No cap - keep adding blocks as long as walkthrough feedback re-enters ALIGN. -->

## Known Issues

<!-- MEDIUM/LOW findings left unresolved when the loop ended. These do not block apply. Empty if none. -->
-

## Sign-off

<!-- Only filled if the user ended the loop early via explicit sign-off (valid only after at least one full walkthrough round). Format: `Cycle <n> -> <user's sign-off statement, verbatim>`. Leave empty if the loop ended via a clean walkthrough round instead. -->
-
